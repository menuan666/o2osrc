<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.shangpu.dao.RunnerDao">
    <insert id="insertRunner">
        INSERT INTO tb_runner(runner_name, phone,runner_desc,
                              create_time, price, user_id)
        VALUES (#{runnerName},#{phone},#{runnerDesc},NOW(),#{price},#{userId})
    </insert>
    <update id="updaterunnerstatus">
        update tb_runner
        <set>
            <if test="status != null">status=#{status},</if>
            <if test="getUserId != null">get_user_id=#{getUserId},</if>
        </set>
        where runner_id=#{runnerId}
    </update>
    <select id="selectrunner" resultType="com.shangpu.entity.Runner">
        SELECT *
        FROM tb_runner
        <where>
            <if test="userId != null">
                and user_id = #{userId}
            </if>
            <if test="getUserId != null">
                and get_user_id = #{getUserId}
            </if>
        </where>
    </select>

</mapper>